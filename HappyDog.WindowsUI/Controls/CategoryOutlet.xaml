<UserControl
    x:Class="HappyDog.WindowsUI.Controls.CategoryOutlet"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HappyDog.WindowsUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    d:DesignHeight="700"
    d:DesignWidth="800">

    <Grid>
        <ScrollViewer ViewChanged="ScrollViewer_ViewChanged">
            <StackPanel>
                <ContentPresenter Content="{x:Bind Jumbotron}"/>
                <StackPanel Style="{StaticResource StackPanelContainer}" Padding="0,20,0,0">
                    <local:ArticleList Articles="{x:Bind Articles}"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
        <controls:Loading IsLoading="{x:Bind IsLoading, Mode=OneWay}" Background="Black" Opacity=".6">
            <ProgressRing IsActive="True" Width="60" Height="60"/>
        </controls:Loading>
    </Grid>
</UserControl>
