<Page
    x:Class="HappyDog.Tools.Views.SvgForWindows10Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HappyDog.Tools.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid Padding="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <CommandBar Background="Transparent"
                        DefaultLabelPosition="Right"
                        OverflowButtonVisibility="Collapsed"
                        HorizontalAlignment="Left">
                <AppBarButton Icon="OpenFile" x:Name="OpenFile" Label="浏览" Click="OpenFile_Click"/>
                <AppBarButton Icon="Copy" x:Name="Copy" Label="复制"/>
                <AppBarButton Icon="Save" x:Name="Save" Label="保存"/>
            </CommandBar>
            <RichEditBox Grid.Row="1" x:Name="Output"/>
        </Grid>
    </Grid>
</Page>
